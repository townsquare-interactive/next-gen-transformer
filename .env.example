TRANSFORMER_API_KEY="" #bearer token for accessing the API routes (required)
CMS_ACCESS_KEY_ID="" #S3 access key ID (required)
CMS_SECRET_ACCESS_KEY_ID="" #S3 secret access key ID (required)
CMS_DEFAULT_REGION="" #S3 region (required)
MAILCHIMP_API_KEY="" #if site is using mailchimp forms (optional)
ZAPIER_URL="" #if site is using zapier forms (optional)
MAKE_URL="" # make.com webhook to allow site to use make for forms (required)
NEXT_PUBLIC_VERCEL_TEAM_ID="" #ID of vercel team (required)
NEXT_PUBLIC_VERCEL_AUTH_TOKEN="" #Vercel auth token (required)
VERCEL_PROJECT_ID="" #ID of vercel project (required)
CREATE_SITE_DOMAINS="1" #Option to publish site domain when creating a landing page (1 or 0)

# Duda API Credentials
DUDA_USERNAME="" #Duda username for the API endpoint.
DUDA_PASSWORD="" #Duda password for the API endpoint.

# Duda Environment Configuration
# Set to '1' to use the sandbox environment (for local/staging),
# or '0' to use the direct environment (for production).
# Example:
# DUDA_USE_SANDBOX=1   # Use sandbox environment for testing
# DUDA_USE_SANDBOX=0   # Use direct environment for production
DUDA_USE_SANDBOX=1      # 1 for sandbox (testing), 0 for direct (production)